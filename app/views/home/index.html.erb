<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <div class="page">
    <div class="buffer">
      <% if alert.present? %>
        <p class="alert alert-success"><%= alert %></p>    
      <% end %>
      
      <%= render partial: 'shared/errors', locals: { note: @note } %>
          
      <%= render 'partials/navbar' %> 

      <%= render 'shared/tags' %>
      <br>

      <%= render 'shared/index_content' %>
    </div>
    <div class="text-center">
      <%= render 'partials/footer' %>
    </div>
  </div>
</body>
</html>