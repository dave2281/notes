<%= render "form", user: @user %>

<div class="text">
  <%= link_to "Show user", @user, class: 'btn btn-primary w-auto' %>
  <%= link_to "Notes", home_index_path, class: 'btn btn-primary w-auto' %>
  <%= link_to "Destroy user", @user, method: :delete, class: 'btn btn-danger w-auto', data: { confirm: 'Are you sure?' } %>
</div>
