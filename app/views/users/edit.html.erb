<h1>Editing user</h1>

<%= render "form", user: @user %>

<br>

<div>
  <%= link_to "Show this user", @user, class: 'btn btn-primary' %>
  <%= link_to "Back to notes", home_index_path, class: 'btn btn-primary' %>
  <%= link_to "Destroy user", @user, method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure?' } %>
</div>
